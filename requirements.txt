# Arbitrage bot dependencies (server.py remains stdlib-only)
websockets>=12.0
cryptography>=42.0
pyotp>=2.9.0
segno>=1.6.0
psycopg[binary,pool]>=3.2.0

# Optional: live Polymarket order placement
py-clob-client-v2>=0.1.0
